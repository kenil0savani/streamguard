name: 'streamguard'
version: '1.0.0'
config-version: 2

profile: 'streamguard'

model-paths: ["models"]

models:
  streamguard:
    staging:
      +materialized: view
      
    marts:
      +materialized: table
      
